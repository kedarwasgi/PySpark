'''
Find wine varieties tasted by 'Roger Voss' and with a value in the 'region_1' column of the dataset. Output unique variety names only.
'''

>>> Solution:

# Import your libraries
import pyspark
from pyspark.sql import functions as F

# Start writing code
df = winemag_p2.filter((F.col("region_1").isNotNull()) & (F.col("taster_name")=="Roger Voss"))
df1 = df.select('variety').distinct()
# To validate your solution, convert your final pySpark df to a pandas df
df1.toPandas()
